---
books-storage.router:
  advertise_uri: localhost:3301
  http_port: 8081

books-storage.s1-master:
  advertise_uri: localhost:3302
  http_port: 8082

books-storage.s1-replica:
  advertise_uri: localhost:3303
  http_port: 8083

books-storage.s2-master:
  advertise_uri: localhost:3304
  http_port: 8084

books-storage.s2-replica:
  advertise_uri: localhost:3305
  http_port: 8085

# books-storage-stateboard:
#   listen: localhost:3310
#   password: passwd
